fn println_i32(number: i32);
fn system_exit(result: i32);

fn main () {
	let a := -(1);
	let b = 6 * 2 - 9 / 3;
	let c = -a + 2;
	let d: int = b * 2 - -3;
	println_i32(a);
	println_i32(b);
	println_i32(c);
	println_i32(d);
	system_exit(0);
}
