fn calc (a: int, b: int) -> int {
	return a / 2 + b;
}

fn main () {
	let a: int = 12;
	let b: int = 3 - calc(8, a);
	print("Value: ");
	print_i32(b);
	print(" (should be -13)\n");
	system_exit(0);
}
