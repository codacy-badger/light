
#run
main :: fn {
    number1 : u64 = 16;
    number2 : u64 = 24;

    number1 = 2 + other(other(number1));
    other(number2);
    other(number1);
}

other :: fn (num: u64) -> u64 {
    _println_u64(num);
    return num + 10;
}
