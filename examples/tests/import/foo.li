
Foo :: struct {
    a1 : s64 = 550;
    a2: Bar;

    #if DEBUG {
        some_other_value : usize;
    }
}

import "bar";

foozle :: fn (number: u64) -> u64 {
    result := bazz.factorial(number * 2);
    result = result + 5;

    return result;
}

bazz :: import "bazz"
