
main :: fn {
    f_result := factorial();
}

factorial :: fn (number: u64) -> u64 {
    if (number == 0) return 1;
    else {
        return factorial(number - 1) + number;
    }
}

foozle :: fn (a: u64, b: u64, c: u64, d: u64) -> u64 {
    return 0;
}
