import! "examples/win_api.li"

Vec2 :: struct {
	x : s32;
	y : s32;
}

factorial :: fn (number: s32) -> s32 {
    return number * 2;
}

NUM :: VAL + 10;

WIN_CALL : type : fn (val: s32) -> s32;

#run
main :: fn {
    c : s32 = a - NUM;
	d := c + 1;

	number := factorial(d);

	cout := GetStdHandle(STD_ERROR_HANDLE);
	//cout = GetStdHandle(STD_OUTPUT_HANDLE);
	//cerr := GetStdHandle(STD_ERROR_HANDLE);
	//WriteFile(cout, "Hello Light!", null, null);
}

a :: 50;

VAL : s32 : -2;
