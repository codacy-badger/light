import! "examples/win32.li"

#run
main :: fn {
	number := factorial(5);
	name1 : string = "This text comes from bytecode!!\n\n";

	if name1 {
		WriteConsole(GetStdHandle(STD_OUTPUT_HANDLE), name1, strlen(name1), 0, 0);
	}
}

factorial :: fn (number: s32) -> s32 {
	asd := 123;
    return number * 2;
}

strlen :: fn (str: string) -> s32 {
	result : s32 = 33;
	return result;
}
